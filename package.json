{
  "name": "cypress-framework",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx cypress run",
    "test:all:headed": "npx cypress run --headed",
    "test:chrome": "npx cypress run --browser chrome",
    "test:config": "npx cypress run --config viewportWidth=1280,viewportHeight=720 --headed",
    "test:specific": "npx cypress run --spec \"cypress/e2e/integration/22-CustomCommands.cy.js\" --headed --browser chrome",
    "test:smoke": "npx cypress run --spec \"cypress/e2e/smoke\" --headed --browser chrome"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@4tw/cypress-drag-drop": "^2.2.5",
    "cypress": "^13.6.2",
    "cypress-real-events": "^1.11.0",
    "dotenv": "^16.3.1"
  }
}